// Единая точка реэкспорта сгенерированного Prisma-клиента (Prisma v7, генератор `prisma-client`).
// Сгенерировать перед запуском: `bun run db:generate`.
// Если поменяешь `output` в schema.prisma — правится только этот путь.
// (Точное имя файла-входа генератора уточнить через context7 /prisma/prisma при недоступности — см. CLAUDE.md.)
export * from '../generated/prisma/client';
